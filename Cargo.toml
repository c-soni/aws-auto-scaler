[package]
name = "auto-scaler"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = { version = "1.0.5", features = ["tokio-native-tls-comp"] }
tokio = { version = "1.50.0", features = ["full"] }
